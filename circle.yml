machine:
  timezone:
    America/Los_Angeles
    
dependencies:
  pre:
    - gem install bundler -v 1.15.1
  override:
    - bundle install:
    
general:
  artifacts:
     - "Gemfile.lock"
